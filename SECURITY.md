# مراجعة أمنية — نقوط (Nuqoot Tracker)

تاريخ المراجعة: ٢٠٢٦-٠٩-١٨ · النطاق: التطبيق (Expo/React Native)، دوال
الحافة، مخطط قاعدة البيانات وسياساتها، سكربتات التشغيل، الاعتماديات.

كل بند أدناه **مُتحقَّق منه في الكود** قبل إدراجه. القسم الأخير يسرد ما
جرى فحصه ولم يُوجد فيه خلل، حتى لا يُعاد فحصه لاحقاً بلا داعٍ.

---

## ١) الثغرات المؤكَّدة، مرتّبة بالخطورة

| # | الخطورة | الموضع | الوصف | الحالة |
|---|---------|--------|-------|--------|
| ١ | **عالية** | `supabase/functions/*` | الدوال الثلاث لم تتحقّق من هوية المُنادي. بوّابة Supabase تقبل أي JWT صالح — والمفتاح العام مُحزَّم داخل التطبيق ويُستخرج منه بسهولة. أي شخص يستخرجه يستنزف رصيد Gemini كاملاً. | أُصلح |
| ٢ | متوسطة | `parse-transaction` | لا سقف لطول `text` ولا لعدد `knownContacts` ولا لحجم الجسم. الطول يُترجم إلى تكلفة مباشرة، والقائمة تدخل المُوجّه نفسه. | أُصلح |
| ٣ | متوسطة | `alerts.ts`, `supabaseError.ts` | رسالة الخطأ المعروضة كانت تضمّ `details` و`hint` من PostgREST — أي بنية قاعدة البيانات. والسجل كان يطبع الكائن كاملاً وأثر النداء في الإنتاج. | أُصلح |
| ٤ | متوسطة | `AuthScreen.tsx` | كلمة المرور: ستة محارف بلا أي شرط تركيب. | أُصلح |
| ٥ | متوسطة | `AuthProvider.tsx` | لا تحديد معدّل على الدخول والتسجيل والضيف وOAuth. | أُصلح |
| ٦ | متوسطة | `schema.sql` (التخزين) | دلو الإيصالات بلا `file_size_limit` ولا `allowed_mime_types`: رفعٌ بلا حدّ، وHTML يُفتح لاحقاً برابط موقّع داخل نطاق التخزين. | أُصلح |
| ٧ | متوسطة | `schema.sql` (RLS) | سياسات `event_participants` و`shared_expenses` و`expense_shares` تتحقّق من ملكية الصفّ لا ممّا يشير إليه. فحص المفتاح الأجنبي لا يمرّ عبر RLS. | أُصلح |
| ٨ | متوسطة | `repository.ts` | لا تحقّق من طول أو شكل أي حقل. و`Number()` تقبل `0x10` و`1e5` بصمت. ومحارف التوجيه ثنائي الاتجاه تمرّ إلى الأسماء. | أُصلح |
| ٩ | متوسطة | `_shared/gemini.ts` | `Access-Control-Allow-Origin: *` ثابتة. | أُصلح |
| ١٠ | متوسطة | بناء الويب ودوال الحافة | لا CSP ولا `nosniff` ولا `Referrer-Policy` ولا `Permissions-Policy`. | أُصلح جزئياً — انظر §٥ |
| ١١ | منخفضة | `supabase.ts` | لا فحص للمخطط على الطلبات الصادرة؛ هبوطٌ إلى `http` يمرّر رمز الجلسة نصّاً صريحاً. | أُصلح |

### تفصيل البند ١ (الأهمّ)

الدوال تُنشر افتراضياً بـ `verify_jwt = true`، فتقبل البوّابة أي JWT صالح
— **بما فيه المفتاح العام نفسه**. لم يكن هناك أي فحص لاحق، فكان كل ما
يلزم لاستهلاك الحساب هو استخراج المفتاح من حزمة التطبيق.

الآن تبدأ كل دالّة بـ `requireUser(request)` التي تسأل
`/auth/v1/user` عن الرمز المُرسل — سؤالُ الخادم لا قراءةُ الادّعاءات
محلياً، لأن القراءة المحلية تصحّ فقط ما دامت البوّابة تتحقّق من التوقيع،
وتسقط صامتةً لو نُشرت الدالّة يوماً بـ `--no-verify-jwt`. يليها
`enforceUserRateLimit(userId)`: اثنتا عشرة نداءة في الدقيقة لكل مستخدم.

---

## ٢) ما جرى فحصه ولم يُوجد فيه خلل

| الفحص | النتيجة | الدليل |
|-------|---------|--------|
| حقن SQL | **غير موجود** | لا `.rpc()` ولا `.or()` ولا `.like()` ولا `.textSearch()` ولا `.filter()` من PostgREST في الشيفرة. كل الاستعلامات عبر `.eq()`/`.in()`، وهي معاملات مُرحَّلة لا نصّ مُركَّب. |
| XSS | **غير موجود** | لا `dangerouslySetInnerHTML` ولا `innerHTML` ولا `eval` ولا `new Function`. `<Text>` في React Native (وreact-native-web) يُخرِج عقدة نصّية لا HTML. |
| اجتياز المسارات في رفع الإيصالات | **غير موجود** | المسار `${user.id}/${Date.now()}.${ext}`؛ `user.id` من الخادم، والامتداد من جدول ثابت يسقط إلى `bin`. لا مدخل مستخدم في المسار. |
| أسرار في المستودع | **غير موجودة** | `scripts/check-secrets.js` يفحص الملفات المتتبَّعة؛ `supabase/.temp/` متجاهَل وغير متتبَّع؛ مفتاح Gemini سرّ خادم لا يصل إلى العميل. |
| خصوصية دلو الإيصالات | **سليمة أصلاً** | الدلو `public = false`، والقراءة برابط موقّع، وثلاث سياسات تحصر كل مستخدم في مجلده. |
| `user_id` من العميل | **سليم أصلاً** | التطبيق لا يرسله إطلاقاً؛ تملؤه `default auth.uid()` وتفرضه RLS. |

### الاعتماديات

`npm audit`: ١٤ تنبيهاً متوسطاً، لا عالي ولا حرج. جذرها تنبيهان:

- **`decode-uri-component`** (عبر `query-string` ← `@react-navigation/core`):
  **لا توجد نسخة مُصلَحة** — النطاق المصاب `<=0.4.2` وهو أحدث ما صدر،
  و`@react-navigation/core` مصاب حتى `<=7.22.0` وهو أحدث ما صدر. الترقية
  لا تُصلحه اليوم.
  **وغير قابل للاستغلال هنا**: `NavigationContainer` بلا خاصيّة `linking`،
  فلا يُمرَّر عنوانٌ خارجي إلى محلّل المسارات أصلاً (رابط OAuth العميق
  يُحلَّل في `AuthProvider` عبر `new URL()`). والأهم: فحص الحزمة المصدَّرة
  لا يجد شيئاً من شيفرة الحزمة المصابة (`singleMatcher`، `multiMatcher`،
  `decodeComponents` — صفر مرّات).
- **`uuid@7`** (عبر `xcode` ← `@expo/config-plugins`): إصلاحه يتطلّب
  تنزيل `expo` إلى الإصدار ٤٦، وهو كسرٌ كامل. وهو **غير مُحزَّم**: الحزمة
  المصدَّرة لا تحوي `xcode` ولا `pbxProject`؛ الاعتمادية تعمل وقت البناء
  (`prebuild`) لا وقت التشغيل.

**القرار**: لا ترقية. الخطران غير قابلين للوصول في الشيفرة المشحونة،
والترقية إمّا غير متاحة أو كاسرة. يُعاد الفحص عند صدور إصلاح.

---

## ٣) ما تغيّر في الشيفرة

**ملفات جديدة**

| الملف | الغرض |
|-------|-------|
| `src/lib/validation.ts` | تحقّق وتعقيم نقيّ بلا اعتماديات: تعقيم النصوص، حدود الطول، المبالغ، العملة، المعرّفات، التواريخ، البريد، سياسة كلمة المرور. |
| `src/lib/validateEntities.ts` | تركيب المتحقّقات على حمولات الكتابة، عند حدّ المستودع. |
| `src/lib/logger.ts` | سجلّ ينقّي الرموز والمفاتيح والبريد والهاتف، ويصمت في الإنتاج. |
| `src/lib/rateLimit.ts` | حاجز محاولات نقيّ (المخزن والساعة محقونان). |
| `src/lib/authThrottle.ts` | ربطه بـ AsyncStorage. |
| `src/lib/captcha.ts` | أنبوب `captchaToken` لمسارات المصادقة. |
| `scripts/check-secrets.js` | يمنع التزام سرّ؛ يفحص المتتبَّع لا القرص. |
| `public/index.html` | قالب الويب مع CSP. |
| `tests/*.test.ts` | ٥٩ اختباراً. |

**ملفات معدَّلة**: `src/lib/{supabase,supabaseError,alerts,repository}.ts`،
`src/store/AuthProvider.tsx`، `src/screens/{AuthScreen,AddSharedExpense,EventParticipants}.tsx`،
`supabase/functions/_shared/gemini.ts`، `supabase/functions/*/index.ts`،
`supabase/schema.sql`، `.env.example`، `tsconfig.json`، `package.json`.

**قرارات تصميم تستحقّ الذكر**

- التحقّق عند **حدّ المستودع** لا في الشاشات: الشاشة بابٌ واحد من عدّة
  أبواب إلى الجدول نفسه (يدوي، ذكي، مسح إيصال)، والمستودع هو المعبر
  الوحيد. ولا يُغني ذلك عن RLS: العميل كلّه تحت يد من يملك الجهاز.
- سياسة كلمة المرور تُفرض على **التسجيل** لا على **الدخول**: فرضُها على
  الدخول كان سيقفل الباب في وجه حسابات أُنشئت بكلمة أقصر، فيمنع صاحبها
  من الدخول ليغيّرها.
- حذف محارف التوجيه ثنائي الاتجاه (U+202A–U+202E وأخواتها) ليس تجميلاً
  في تطبيق عربي: U+202E وحده يقلب عرض ما بعده، فيظهر الاسم أو المبلغ
  للعين غير ما هو مخزَّن.
- التحقّق يرفض ما تجاوز الحدّ ولا يقصّه: القصّ الصامت يفقد المستخدم
  بياناته دون أن يعلم.

---

## ٤) نتائج التحقّق

| الفحص | النتيجة |
|-------|---------|
| `npx tsc --noEmit` (التطبيق) | نظيف |
| `tsc` على دوال الحافة (مع تصريح Deno) | نظيف |
| `npm test` | ٥٩/٥٩ ناجح |
| `node scripts/check-secrets.js` | لا أسرار — وتأكّد بضبط شاهد: مفتاح وهمي مُدرَج يُرصد ويُرجِع ١ |
| `npm run export:web` | نجح |
| بناء الويب في Chromium تحت CSP | صفر مخالفات CSP، صفر أخطاء صفحة، الصفحة تُرسَم |
| `npm run check:supabase` | الإعداد سليم محلياً؛ النداء الحقيقي محجوب من بيئة المراجعة |

**اختبار وجد عطباً حقيقياً**: اختبار «النجاح يمسح السجل» كشف أن
`{...EMPTY}` في `rateLimit.ts` نسخٌ سطحي، فتبقى مصفوفة `failures` مشتركة
بالمرجع بين كل السلال — أي تتسرّب إخفاقات مستخدمٍ إلى آخر ويُقفل حساب لم
يُخطئ. أُصلح بدالّة `emptyBucket()`.

---

## ٥) ما يلزم منك خارج المستودع

هذه لا يمكن إنجازها من الشيفرة، وبعضها يُبطل أثر ما سبق إن أُهمل.

### لوحة Supabase

1. **ارفع الحدّ الأدنى لكلمة المرور** إلى ١٠ على الأقل:
   Authentication ← Providers ← Email ← Minimum password length.
   *بدونه يبقى الخادم أضعف من الواجهة*، ويقبل كلمةً ترفضها الشاشة إن وصل
   إليه طلب من خارج التطبيق.
2. **فعّل تسريب كلمات المرور المعروفة**: Authentication ← Attack
   Protection ← Leaked password protection (يقابل قاعدة HaveIBeenPwned).
3. **فعّل CAPTCHA** إن كان التسجيل مفتوحاً للعامّة: Attack Protection ←
   Enable Captcha protection. ثم اضبط
   `EXPO_PUBLIC_SUPABASE_CAPTCHA_SITE_KEY`، وركّب الودجة عبر
   `setCaptchaProvider` (انظر الملاحظة أدناه).
4. **راجع Redirect URLs**: احصرها في أصولك الحقيقية فقط.
5. **شغّل `supabase/schema.sql`** من SQL Editor لتطبيق تشديد RLS وحدود
   الدلو. الملف قابل لإعادة التشغيل.
6. **اضبط أصول دوال الحافة**:
   `supabase secrets set ALLOWED_ORIGINS="https://your-app.example,http://localhost:8081"`
   غير مضبوط = أي أصل (سلوك التوافق).

### استضافة بناء الويب

CSP مضبوطة في `public/index.html` ومُتحقَّق منها. لكن ثلاث ترويسات **لا
تعمل من وسم `<meta>`** ويجب ضبطها على المستضيف:

```
Strict-Transport-Security: max-age=63072000; includeSubDomains; preload
X-Content-Type-Options: nosniff
Content-Security-Policy: frame-ancestors 'none'
Referrer-Policy: strict-origin-when-cross-origin
Permissions-Policy: camera=(), microphone=(), geolocation=()
```

وكذلك: **عطّل سرد المجلدات** (directory listing) على المستضيف — لا يمكن
ضبطه من المستودع. وفعّل HTTPS وإعادة التوجيه الإجباري إليه.

### CAPTCHA — ما لم يُنجَز ولماذا

الأنبوب كامل: متى ضُبط مفتاح الموقع، يُطلب رمز التحقّق في كل مسار مصادقة.
**الودجة نفسها لم تُركَّب**: عرض hCaptcha داخل React Native يحتاج
`react-native-webview`، وهي اعتمادية ثقيلة لا يصحّ فرضها على مشروع قد لا
يُفعّل الميزة. متى فعّلتها، ركّب الودجة واستدعِ `setCaptchaProvider` مرّة
عند الإقلاع، فيسري الرمز على كل المسارات دفعةً واحدة.

### فحص RLS الحقيقي

الاختبارات تؤكّد المخطط نصّاً لا سلوكاً (بيئة الاختبار بلا قاعدة بيانات).
للتأكّد عملياً، من SQL Editor بحسابين مختلفين:

```sql
-- بجلسة المستخدم (ب)، والمعرّف صفٌّ يملكه (أ):
select * from public.transactions where id = '<معرّف صفّ يملكه أ>';
-- المتوقّع: صفر صفوف (لا خطأ صلاحيات — RLS تُخفي ولا تعترض).

insert into public.expense_shares (expense_id, share_amount)
values ('<مصروف يملكه أ>', 10);
-- المتوقّع بعد التشديد: رفض بـ new row violates row-level security policy.
```

---

## ٦) ما بقي مفتوحاً

- **تحديد المعدّل في دوال الحافة داخل الذاكرة**: العزلات متعدّدة وقصيرة
  العمر، فهو يوقف الفيضان المتّصل من مصدر واحد لا التوزيع البطيء. الحلّ
  الكامل جدولٌ في قاعدة البيانات أو بوّابة أمام الدوال.
- **حاجز المحاولات في التطبيق على الجهاز**: من يملك الجهاز يملك التخزين.
  الحاجز الحقيقي حدود Supabase وCAPTCHA؛ هذا طبقةٌ فوقهما.
- **`'unsafe-inline'` في `style-src`**: ضرورة تقنية — react-native-web
  وNativeWind يحقنان الأنماط وقت التشغيل. لا يقابلها خطر XSS هنا لعدم
  وجود أي مسار يُدرج HTML من مدخلات المستخدم، لكنها تبقى تنازلاً يجب
  إعادة النظر فيه إن أُضيف عرضُ محتوى غنيّ يوماً.
