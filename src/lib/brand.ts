/**
 * اسم التطبيق، في موضع واحد.
 *
 * كان الاسم مكتوباً حرفياً في سبعة مواضع — الشعار، شاشة الدخول، شاشة
 * القفل، اسم المُعتمِد في WebAuthn، ونصوص البصمة — فتبديله كان يعني
 * تتبّعه فيها جميعاً، وما يُنسى منها يبقى معروضاً بالاسم القديم.
 *
 * وما يبقى خارج هذا الملف اضطراراً: `app.json` و`public/index.html`
 * و`public/manifest.json` — ملفات ساكنة لا تستورد شيئاً. يمسك
 * `tests/brand.test.ts` هذه المواضع ويقارنها بما هنا، فانحرافُ أحدها
 * يُسقط الاختبار بدل أن يظهر عند المستخدم.
 */

/** الاسم كما يُعرض في كل مكان. */
export const APP_NAME = 'الكراسة الصفرا';

/** السطر تحت الاسم في الشعار الكامل. */
export const APP_TAGLINE = 'دفتر النقوط والواجبات';

/**
 * وصف قصير لبطاقات المشاركة ولملف الويب.
 *
 * جملة واحدة: ما يظهر في معاينة رابطٍ على واتساب أو تويتر يُقتطع بعد
 * سطرين، والجملة الثانية لا تصل غالباً.
 */
export const APP_DESCRIPTION =
  'دفتر النقوط والواجبات: سجّل ما دفعته وما استلمته، واعرف ما لك وما عليك عند كل شخص.';

/** معرّف لاتيني للمواضع التي لا تقبل العربية (WebAuthn، مفاتيح تقنية). */
export const APP_HANDLE = 'karrasa';
