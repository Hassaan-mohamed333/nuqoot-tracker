/**
 * ألوان التطبيق — وضع فاتح واحد.
 *
 * لماذا يوجد هذا الملف أصلاً: أصناف Tailwind تغطّي كل ما يُنسَّق بـ
 * `className`، لكن أشياءَ لا تقبلها — لون أيقونة lucide، ومؤشّر التحميل،
 * وخصائص سمة التنقّل، وتعبئة SVG — تحتاج القيمة نصّاً. وقيم متغيّرات CSS
 * غير مقروءة من جافاسكربت في React Native، فتُكتب هنا يدوياً.
 *
 * **يجب أن تبقى مطابقة لرموز `global.css`**: هما مصدران لنفس اللوحة،
 * وانحرافُ أحدهما عن الآخر يظهر أيقونةً بلونٍ لا يشبه ما حولها.
 *
 * ثابت لا خطّاف: لم يعد هناك وضعٌ ليلي ولا تفضيلٌ محفوظ، فلا حالة
 * تُراقَب ولا سياق يُقرأ. `usePalette()` السابق كان يقرأ سياق السمة؛
 * وبزواله صار استيراد الثابت أصدق وأخفّ — ولا يُعيد رسم شيء.
 */
export const palette = {
  /**
   * الكهرماني يحمل الإجراء الرئيسي، و`primaryStrong` نصُّه وأيقوناته.
   *
   * الفصل ليس ترفاً: #F59E0B فوق الأبيض تباينه ‎2.1:1‎، فلا يصلح لوناً
   * لأيقونةٍ ولا لنصّ. وكل ما كان يستعمل `primary` لوناً لعنصر فوق سطح
   * فاتح صار يستعمل `primaryStrong` (‎7.0:1‎ فوق الأبيض، ‎6.2:1‎ فوق
   * السطح المرتفع الدافئ).
   */
  primary: '#F59E0B',
  primaryStrong: '#92400E',
  accent: '#D97706',
  secondary: '#003566',
  success: '#047857',
  danger: '#BE123C',
  warning: '#C2410C',
  base: '#FAF8F3',
  surface: '#FFFFFF',
  surfaceRaised: '#F5F1E8',
  border: '#E8E2D5',
  text: '#0F172A',
  muted: '#57534E',
  subtle: '#78716C',
  onPrimary: '#0F172A',
  onSecondary: '#FFFFFF',
  onAccent: '#0F172A',
} as const;

export type Palette = typeof palette;

/**
 * ألوان شارة الشعار.
 *
 * منفصلة عن اللوحة عمداً: الشارة لوحٌ كهرماني عليه رسمٌ داكن، وهي هوية
 * التطبيق لا سمته — أيقونة التطبيق على الجهاز لا تتبدّل، فلا يصحّ أن
 * يتبدّل مقابلها داخل الشاشة.
 *
 * وكانت لوحاً كحلياً برسمٍ ذهبي، فانقلبت مع الاسم: «الكراسة الصفرا»
 * شارتها صفراء، لا صفراءَ على كحلي.
 */
export const BRAND = {
  /** تدرّج اللوح: من الكهرماني الفاتح إلى العميق. */
  badgeFrom: '#FBBF24',
  badgeTo: '#D97706',
  /** الرسم فوق اللوح: داكن، فالرسم الفاتح فوق الكهرماني لا يُرى. */
  badgeMark: '#0F172A',
} as const;
